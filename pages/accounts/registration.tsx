import { AccountFormWrapper } from "../../components";

interface HomeProps {}

const Home: React.FC<HomeProps> = (props) => {
  return (
    <AccountFormWrapper>
      <h1>Registration</h1>
    </AccountFormWrapper>
  );
};

export default Home;
